/**
 * Set to "owner/repo" once this project's own GitHub repo is public, so its quiz-data/ folder
 * shows up by default for everyone on the Browse page. Leave empty to require every visitor to
 * type a repo themselves.
 */
export const DEFAULT_REPO = '';
